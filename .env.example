# 联机模式后端 API 地址（可选）。
# 默认与页面同源；后端部署到独立域名 / 端口时，取消注释并指定：
# VITE_API_BASE=https://api.example.com

# OAuth 后端与前端分域部署时，浏览器请求必须携带 HttpOnly Cookie；代码已默认启用 credentials。

# 血流普通 AI：首胡自摸的残局路线机会约束默认开启。
# 回退到接入前策略：取消注释并重新启动开发服务／重新构建部署。
# VITE_BLOOD_FLOW_ROUTE_OPPORTUNITY=off

# 血流普通 AI：吃／碰后的番型估值默认包含新副露。
# 回退评估口径（需重启／重新构建）：
# VITE_BLOOD_FLOW_CLAIM_MELD_PROJECTION=off

# 血流本地 AI：对手大牌风险定价（off 旧口径 vs tier 档位版）双臂对拍 + 决策级分歧诊断

规则：lotus-blood-flow-v1；基线提交：65c49432182025cb6fb8f0dbf20d950714240444（记录时 `src/` 工作区**含未提交改动**，即对手风险定价特性本身，因此提交号不足以标定被测源码，请看文末"被测源码指纹"）；种子：1～20（共 20 局 × 2 臂 = 40 局）；四席同一策略（贪婪 EV `decideBloodFlowActionEv`）自对局，跑到结算；显式关闭 UI 节拍（`winBeatMs: 0`、`now: () => 0`）；每个动作后检查 136 张物理牌守恒与积分零和。

- **A 臂（改前）**：`{ ...BLOOD_FLOW_AI, opponentPatternRisk: 'off' }` —— 弃牌放炮成本只由该牌公开张数决定（0 张 0.25 / 1 张 0.1 / ≥2 张 0 档 × 40 点）。
- **B 臂（档位版，默认）**：`BLOOD_FLOW_AI` —— 在上述基础上乘以"对手在做大牌"的风险倍率（tier1 ×4 / tier2 ×16 / tier3 ×32，染手非嫌疑花色再 ×0.5；已锁手家现物折扣失效，见 `src/game/shared/ai/opponentPatternRisk.ts`）。
- **高风险判定尺子**：两臂都用 `bloodFlowOpponentRisk(view, BLOOD_FLOW_AI)` 复算"弃牌者视角下三家对手的最高档"，`tier ≥ 2` 记为高风险。A 臂自身配置不产生档位，所以量度必须固定用档位口径，否则两臂占比不可比。

## 方法

1. 每个种子 `1..20` 用 `seededRandom(seed)` 洗牌，两臂**同种子同开局**（各 2000 分、庄家 0 席）各跑一局。
2. 决策循环与 `src/game/variants/lotus/bloodFlow/evStrategy.test.ts` 一致：`bloodFlowSeatView` → `decideBloodFlowActionEv(view, 臂配置)` → `engine.submit`；唯一差别是传入的 `BLOOD_FLOW_AI` 配置。
3. 当某次提交真的产生弃牌（`engine.discardActions` 增长）时，把**出牌前**那一刻的公共风险快照按弃牌源事件 id 存档；结算时凡 `batch.source.kind === 'discard'` 的批次就是点炮，用该快照回填"当时弃牌者眼中的最高档"。
4. 四家锁手的判定完全由批次顺序重建（`record.ordinal` = 该家本次胡之前的已胡次数 + 1），不读引擎私有状态；"四家锁手后仍然胡牌"= 该批次成立前四家 `winCount` 均已 ≥1（锁手后"已胡仍付款"，这些胡仍然产生收付）。
5. 分位数为最近秩（nearest-rank）。
6. **决策级诊断口径**（本轮新增）：
   - *决策序列最长公共前缀*：每个种子把两臂各自每一步的动作按顺序记成序列，公共前缀长度 = 两臂从第一步起连续多少个动作完全相同；首个分歧步 = 前缀长度 + 1。因为两臂开局相同，这一步就是两条对局轨迹的分岔点。
   - *同局面双判*：驱动某一臂的每一步时，在**同一份公共视图**上再让另一臂决策一次（两个决策函数都是纯函数，不会改变本臂轨迹），统计"同局面下两臂选择不同"的步数与占比。这是"定价是否真的改变选择"的直接频率指标。
   - *弃牌局面风险项诊断*：在本家 14 张且有多个合法弃牌候选的局面，比较两臂放炮成本在候选牌上的形态——是否对全部候选恒定（等价于"现物 / 公开张数折扣整体失效"，风险项此时不携带候选间差异），以及"成本最低的那张牌"是否发生变化（风险项自身的排序是否变化）。
7. **确定性自检**：两臂跑完后，用同样种子把前 2 个种子各重跑一遍，断言决策序列与点炮赔付序列逐位一致（脚本内 `expect`）。

## 数据表（端局指标）

| 指标 | A 臂（`opponentPatternRisk: 'off'`，改前口径） | B 臂（`'tier'`，档位版） | Δ(B−A) |
|---|---:|---:|---:|
| 局数 | 20 | 20 | 0 |
| 决策步数（本地 AI 被调用次数） | 2542 | 2540 | -2 |
| 平均每局总胡次数（含锁手重复胡） | 35.40 | 36.75 | +1.35 |
| 平均每局胡牌批次数 | 30.60 | 32.05 | +1.45 |
| 平均每局净分变化（带符号，零和恒 0） | 0.00 | 0.00 | 0.00 |
| 平均每局每席 |净分变化| | 523.00 | 515.25 | -7.75 |
| 平均每局终局最大分差 | 1406.00 | 1382.00 | -24.00 |
| 平均首胡墙余 | 47.05 | 47.10 | +0.05 |
| 无胡局数 | 0 | 0 | 0 |
| 全桌弃牌动作总数 | 1433 | 1431 | -2 |
| 点炮批次（source=discard） | 385 | 418 | +33 |
| 点炮胡牌记录（一炮多响按记录计） | 481 | 512 | +31 |
| 点炮率（点炮批次 / 全桌弃牌）% | 26.87 | 29.21 | +2.34 |
| 点炮赔付：每单家支付均值 | 16.59 | 17.40 | +0.81 |
| 点炮赔付：每单家支付 P50 | 10.00 | 20.00 | +10.00 |
| 点炮赔付：每单家支付 P90 | 30.00 | 30.00 | 0.00 |
| 点炮赔付：每单家支付最大 | 40 | 50 | +10 |
| 点炮赔付：每批总收均值 | 20.73 | 21.32 | +0.59 |
| 点炮赔付：每批总收 P50 | 20.00 | 20.00 | 0.00 |
| 点炮赔付：每批总收 P90 | 40.00 | 40.00 | 0.00 |
| 点炮赔付：每批总收最大 | 60 | 90 | +30 |
| 点炮给高风险档对手（当时最高档≥2）次数 | 381 | 416 | +35 |
| 点炮给高风险档对手占比 % | 98.96 | 99.52 | +0.56 |
| 高风险点炮批次总收均值 | 20.71 | 21.27 | +0.57 |
| 高风险点炮批次总收最大值 | 60 | 90 | +30 |
| 高风险点炮：最高档只由"已胡N次仍听"触发 | 129 | 135 | +6 |
| 高风险点炮：最高档含副露/牌河等非锁手信号 | 252 | 281 | +29 |
| 高风险点炮中"胡家本身即高风险档"批次数 | 375 | 408 | +33 |
| 点炮最高档分布：tier0 | 3 | 1 | -2 |
| 点炮最高档分布：tier1 | 1 | 1 | 0 |
| 点炮最高档分布：tier2 | 357 | 374 | +17 |
| 点炮最高档分布：tier3 | 24 | 42 | +18 |
| 被抢杠胡次数 | 0 | 0 | 0 |
| 杠上开花次数 | 0 | 0 | 0 |
| 四家锁手后仍然胡牌的批次 | 23 | 24 | +1 |
| 四家锁手后仍然胡牌的记录 | 28 | 29 | +1 |
| 已锁手座位再次胡牌（ordinal>1）记录 | 656 | 679 | +23 |

补充：两臂"点炮时弃牌者视角最高档"的信号词频（说明为什么会被判成高风险）——

- A 臂：牌河几乎未打条 ×113、牌河中张密集 ×64、已胡1次仍听 ×56、副露半染手 ×47、副露染手嫌疑 ×45、已胡2次仍听 ×36、牌河无字牌幺九 ×36、已胡6次仍听 ×34、已胡4次仍听 ×32、已胡3次仍听 ×30、已胡5次仍听 ×28、已胡9次仍听 ×27（其余 61 种低频信号略）
- B 臂：牌河几乎未打条 ×109、牌河中张密集 ×80、副露半染手 ×66、已胡1次仍听 ×56、牌河无字牌幺九 ×44、副露染手嫌疑 ×43、牌河未打条 ×42、已胡2次仍听 ×41、已胡5次仍听 ×34、已胡7次仍听 ×34、已胡6次仍听 ×33、已胡3次仍听 ×31（其余 61 种低频信号略）

容易被忽略的两项分母：A 臂点炮批次 385 / 全桌弃牌 1433（26.87%），B 臂点炮批次 418 / 全桌弃牌 1431（29.21%）；点炮批次里最高档 ≥2 的：A 98.96% / B 99.52%。

## 决策级分歧诊断

### 1. 逐种子决策序列（最长公共前缀）

| 种子 | 决策步数 A / B | 最长公共前缀 | 首个分歧步：A vs B |
|---:|---:|---:|---|
| 1 | 140 / 140 | 140（无分歧） | — |
| 2 | 113 / 114 | 41 | 42：A={"kind":"discard","index":1}（座3/turn，墙52） vs B={"kind":"discard","index":11}（座3/turn，墙52） |
| 3 | 106 / 109 | 37 | 38：A={"kind":"discard","index":8}（座0/turn，墙49） vs B={"kind":"discard","index":1}（座0/turn，墙49） |
| 4 | 137 / 131 | 33 | 34：A={"kind":"discard","index":5}（座2/turn，墙51） vs B={"kind":"discard","index":11}（座2/turn，墙51） |
| 5 | 151 / 151 | 61 | 62：A={"kind":"discard","index":10}（座0/turn，墙47） vs B={"kind":"discard","index":3}（座0/turn，墙47） |
| 6 | 124 / 113 | 39 | 40：A={"kind":"discard","index":13}（座2/turn，墙51） vs B={"kind":"discard","index":10}（座2/turn，墙51） |
| 7 | 126 / 126 | 126（无分歧） | — |
| 8 | 102 / 102 | 36 | 37：A={"kind":"discard","index":0}（座2/turn，墙50） vs B={"kind":"discard","index":1}（座2/turn，墙50） |
| 9 | 139 / 140 | 50 | 51：A={"kind":"discard","index":10}（座2/turn，墙43） vs B={"kind":"discard","index":7}（座2/turn，墙43） |
| 10 | 112 / 111 | 38 | 39：A={"kind":"discard","index":13}（座3/turn，墙46） vs B={"kind":"discard","index":5}（座3/turn，墙46） |
| 11 | 134 / 133 | 61 | 62：A={"kind":"discard","index":3}（座3/turn，墙38） vs B={"kind":"discard","index":7}（座3/turn，墙38） |
| 12 | 136 / 129 | 42 | 43：A={"kind":"discard","index":12}（座1/turn，墙52） vs B={"kind":"discard","index":4}（座1/turn，墙52） |
| 13 | 140 / 142 | 52 | 53：A={"kind":"discard","index":4}（座0/turn，墙45） vs B={"kind":"discard","index":6}（座0/turn，墙45） |
| 14 | 109 / 100 | 37 | 38：A={"kind":"discard","index":13}（座1/turn，墙52） vs B={"kind":"discard","index":0}（座1/turn，墙52） |
| 15 | 143 / 144 | 86 | 87：A={"kind":"discard","index":6}（座3/turn，墙29） vs B={"kind":"discard","index":7}（座3/turn，墙29） |
| 16 | 139 / 139 | 139（无分歧） | — |
| 17 | 108 / 115 | 42 | 43：A={"kind":"discard","index":5}（座1/turn，墙47） vs B={"kind":"discard","index":0}（座1/turn，墙47） |
| 18 | 154 / 154 | 48 | 49：A={"kind":"discard","index":4}（座3/turn，墙51） vs B={"kind":"discard","index":5}（座3/turn，墙51） |
| 19 | 122 / 139 | 39 | 40：A={"kind":"discard","index":3}（座1/turn，墙55） vs B={"kind":"discard","index":7}（座1/turn，墙55） |
| 20 | 107 / 108 | 18 | 19：A={"kind":"discard","index":2}（座0/turn，墙64） vs B={"kind":"discard","index":4}（座0/turn，墙64） |

汇总：**17/20** 个种子出现决策分歧（其中**首步即分歧 0 个**），**3/20** 个种子整局序列完全相同。
这意味着端局指标（点炮次数、赔付分布）在多数种子上是"同一条轨迹"，两臂的差别只能通过少数分岔点传播。

### 2. 同局面双判（同一公共视图上两臂各选一次）

| 轨迹 | 决策步数 | 两臂选择不同的步数 | 占比 |
|---|---:|---:|---:|
| A 臂（off）轨迹 | 2542 | 61 | 2.40% |
| B 臂（tier）轨迹 | 2540 | 77 | 3.03% |

### 3. 弃牌局面风险项形态

| 轨迹 | 弃牌局面数 | 档位成本对全部候选恒定 | 成本最低的候选发生变化 |
|---|---:|---:|---:|
| A 臂（off）轨迹 | 1246 | 468（37.56%） | 202（16.21%） |
| B 臂（tier）轨迹 | 1237 | 484（39.13%） | 190（15.36%） |

### 4. 定向微场景（固定局面，不走整局）

### 微场景：嫌疑花色差异化（副露染手）

- 固定局面手牌：`m1,m2,m3,m4,m5,m6,m7,s2,s3,east,south,p1,p5,p9`；对手档位：`[{"seat":1,"tier":2,"suspectSuit":"p","signals":["副露染手嫌疑"]},{"seat":2,"tier":0,"suspectSuit":null,"signals":[]},{"seat":3,"tier":0,"suspectSuit":null,"signals":[]}]`
- `off` 选 **p1**（index 11），该牌旧口径放炮成本 **4 点**
- `tier` 选 **m1**（index 0），该牌档位口径放炮成本 **0 点**；档位成本对全部候选是否恒定：**false**
- 两臂选择是否不同：**true**

### 微场景：锁手家不吃现物折扣

- 固定局面手牌：`m1,m1,m1,m2,m3,m4,s7,s8,s9,east,south,p1,p5,p9`；对手档位：`[{"seat":1,"tier":2,"suspectSuit":"p","signals":["牌河几乎未打筒","牌河中张密集","已胡5次仍听"]},{"seat":2,"tier":0,"suspectSuit":null,"signals":[]},{"seat":3,"tier":0,"suspectSuit":null,"signals":[]}]`
- `off` 选 **east**（index 9），该牌旧口径放炮成本 **0 点**
- `tier` 选 **s7**（index 6），该牌档位口径放炮成本 **160 点**；档位成本对全部候选是否恒定：**true**
- 两臂选择是否不同：**true**

## 结论

### 1. 哪一臂更少"点炮给大牌"：本样本给不出肯定答案，表面差值的方向与预期相反
- 点炮给高风险档对手（当时最高档 ≥2）：A 臂 381 次、B 臂 416 次（Δ+35），占该臂全部点炮批次 98.96% → 99.52%。
- 全部点炮批次（source=discard）：A 385 → B 418（Δ+33，即 B 臂反而多 33 次，相当于 8.57%）；点炮率 26.87% → 29.21%。**没有观察到"B 臂更少点炮给大牌"**；方向反而是 B 臂略多、但差值远小于噪声。
- 赔付分布几乎不动：每单家支付均值 16.59 → 17.40 点，P50 10 → 20，P90 30 → 30，最大 40 → 50；每批总收均值 20.73 → 21.32，最大 60 → 90。
- 代价项也没有出现系统性方向：每局总胡次数 35.40 → 36.75，每席 |净分变化| 523.00 → 515.25，终局最大分差均值 1406.00 → 1382.00，平均首胡墙余 47.05 → 47.10，四家锁手后仍胡牌批次 23 → 24（即"代价"同样看不出）。
- **读法**：这些 Δ 不是配对差分（见文末"口径与边界"），幅度也远小于本样本的局间波动（每席 |净分变化| 达 523 点级），所以既不能宣布"档位版更少点炮给大牌"，也不能宣布"档位版更危险"。要回答这个问题必须换方法（见第 4 节）。
- 完全一致的两项：被抢杠胡 0/0、杠上开花 0/0（本批 40 局里这两类事件为 0 次，样本量不足以比较）。
- 另一个必须点明的问题："点炮给高风险档对手"这个指标在本规则下**几近饱和**（98.96% / 99.52%）——血流里任何已胡过的家都已经是 tier 2（`已胡N次仍听`），所以它几乎没有分辨力，不应单独当安全性的判据。

### 2. 决策级分歧：档位版确实改变选择，但只占极少数局面
- 20 个种子中 17 个出现决策分歧（首步即分歧 0 个），3 个整局决策序列完全相同 ⇒ 端局指标在多数种子上就是"同一条轨迹"，两臂差别只能通过少数分岔点向下传播。
- 同局面双判（同一份公共视图上两臂各选一次）：A 臂轨迹 2542 步中 61 步（2.40%）选择不同；B 臂轨迹 2540 步中 77 步（3.03%）选择不同。
- 定向微场景是"档位版生效"的直接证据（固定局面、不走整局、脚本内 `expect` 断言）：嫌疑花色场景 `off` 打 p1、`tier` 改打 m1（嫌疑花色生张定价 4 → 64 点、非嫌疑 4 → 32 点，边际翻转）；锁手现物场景 `off` 打 east、`tier` 改打 s7（现物从 0 点变成统一 160 点，折扣失效）。口径一旦变动，这两条断言会直接失败。

### 3. 假设检验：成立（并定位到具体失效环节）
待检验假设：**tier≥2 的两类信号（`副露染手嫌疑` / `已胡N次仍听`）在血流对局里近乎常态存在，导致风险倍率大多只是把整手牌的价格整体抬高或整体拉平，对"候选之间的相对排序"影响有限，因此端局指标趋同；真正会改变选择的是"嫌疑花色 vs 非嫌疑花色"和"锁手家不吃现物折扣"这两类差异化信号。**
逐条数据：
- **前提成立**：A 臂高风险点炮批次里，最高档"只"由 `已胡N次仍听` 触发的占 33.86%，其余 66.14% 含副露 / 牌河信号——"已锁手"是与副露信号规模相当的常态来源，而不是罕见信号。
- **"整手牌价格被拉平"成立**：弃牌局面中档位放炮成本对**全部合法候选相同**（不携带任何候选间差异）的占比：A 臂轨迹 37.56%、B 臂轨迹 39.13%。这些局面里档位项只可能"抹平"旧口径的现物折扣，无法把风险"导向"某一张牌。
- **风险项自身排序受影响的比例不大**：档位口径下"放炮成本最低的候选牌"发生变化的局面占 A 16.21% / B 15.36%。
- **真正传到决策的只有极小一部分**：同局面双判差异率 A 2.40% / B 3.03%。风险项即使自身排序变了，也要跨过"听牌 / 进张 / 番型潜力"这几项主导 netScore 的量级才能翻转选择——候选之间本来就常差几十点以上的攻击分。
结论：**假设成立**。档位倍率确实生效（微场景已直接证明），但它的作用面是"少数局面里的差异化信号"：约一半局面里它退化成整手牌的统一涨价（现物折扣失效），另一半才有花色差异化，最终改变弃牌选择的不足全部决策的 1%。因此 20 局级样本的端局聚合指标不可能对它有反应——评估定价效果必须依赖决策级分歧统计与定向微场景，而不是端局数值。

### 4. 可疑之处与后续建议
- 端局指标的差值不是配对差分：一旦分岔，两臂后续牌局完全不同，Δ 混有"轨迹不同"的噪声，不能用显著性解释，也不应据此宣布"哪一臂更安全"。
- `已胡N次仍听` 直接把锁手家定成 tier 2 并使现物折扣失效，等于让"任何已胡对手"的价格对所有候选牌相同（本样本 37.56% 的弃牌局面如此）；若目标是"更精细地回避大牌"，这里的分辨率可能不足——锁手家的档位不区分其真实番型，且"高风险"在点炮批次里 99.0% 命中，几乎不筛选。这是设计观察，本脚本只做记录，未改任何规则。
- 本批 40 局（20 局 × 2 臂）里被抢杠胡与杠上开花都是 0 次，无法比较；要评估这两类事件需要更大样本或条件化夹具。
- 若要把"档位版是否更少点炮给大牌"做成可信结论，建议：固定两臂共用同一批**分岔后重放**的对局（A 臂某一手决策替换为 B 臂策略再继续，其余完全相同），或把样本提到数百局并报置信区间；当前脚本已提供可复现的骨架与决策级指标。

## 两臂完全一致的指标（如实列出）

- 局数
- 平均每局净分变化（带符号，零和恒 0）
- 无胡局数
- 点炮赔付：每单家支付 P90
- 点炮赔付：每批总收 P50
- 点炮赔付：每批总收 P90
- 点炮最高档分布：tier1
- 被抢杠胡次数
- 杠上开花次数

注意：即使某些端局指标两臂数值相同，决策级分歧统计（上表）显示的选择差异仍然存在——数值相同只说明这些分歧没有传播到该指标。

## 复现命令与耗时

```
pnpm exec vitest run --dir scripts opponent-risk-report.test.ts
```

- `--dir scripts` 不能省：根 `vite.config.ts` 里 `test.dir: './src'`，只写 `pnpm exec vitest run scripts/opponent-risk-report.test.ts` 会得到 `No test files found`。
- 默认种子 1～20，耗时约 95.6 秒（A 臂 71.0s + B 臂 24.6s，含决策级双判与确定性自检，本机实测，随机器浮动）。
- **两臂耗时差不要当性能结论**：A 臂（旧口径）明显更慢，但实测是同一进程内的缓存 / JIT 顺序效应——对同一步局面先后调用两种口径计时，顺序为「off 先 / tier 后」时得到 4.81ms / 0.70ms，顺序反转为「tier 先 / off 后」时得到 0.30ms / 0.28ms（同种子重跑整臂也从 16.2s 降到 0.7s）。同一进程内先跑的配置承担预热成本，与配置本身的算法开销无关。
- 该文件在 `scripts/` 下，`pnpm test`（只跑 `src`）不会执行它，因此不进默认回归、也不进 CI 全量；需要时手动跑。
- 调整样本量：`OPPONENT_RISK_SEEDS=<n> pnpm exec vitest run --dir scripts opponent-risk-report.test.ts`（默认 20；改小后请在本节注明实际局数）。
- 脚本同时写出 `work/opponent-risk-report.json`（机器可读的完整数字）并重写本文件，因此本文档中的数字与代码运行结果始终同源。
- 脚本只读生产代码（`src/**` 未做任何修改），不改变规则、计分、封顶与锁手行为。

### 被测源码指纹（sha256 前 12 位）

| 文件 | 指纹 |
|---|---|
| `src/game/shared/ai/opponentPatternRisk.ts` | `5b532d067200` |
| `src/game/variants/lotus/bloodFlow/ai.ts` | `78401c2bfd9b` |
| `src/game/variants/lotus/bloodFlow/config.ts` | `a9d30a85bc68` |
| `src/game/variants/lotus/bloodFlow/evContext.ts` | `735e77629d1f` |
| `src/game/variants/lotus/lotusAi.ts` | `112934c1fff4` |
| `src/game/variants/lotus/bloodFlow/engine.ts` | `8f0f7bcad50a` |

`src/` 工作区在记录时**有未提交改动**。风险定价算法或 EV 决策路径一旦被改动，上表数字即失效——重跑本脚本即可（会自动重写本文档与 `work/opponent-risk-report.json`）。

## 口径与边界

- 固定种子小样本，只能看**方向与量级**，不能推出长期平衡、胜率或"哪种口径更强"的结论。
- 两臂同种子同开局，但决策一旦分歧，后续牌河 / 副露 / 摸牌顺序就完全不同，所以本质是"同分布对照"，不是逐局配对的差分实验；端局指标的差值不是配对差分。
- 「平均每局净分变化」带符号恒为 0（四人零和、开局各 2000），真正有信息量的是每席 |净分变化| 与终局最大分差两行。
- 「点炮给高风险档对手」用**最高档**（三家取最大）而非"实际胡家那一家"的档位，这是任务口径；两者都列在表里。
- 同局面双判沿各自轨迹取样：分岔之前的局面才是严格同一局面，分岔之后两臂面对的牌河已不同，因此它衡量的是"分歧频率"，不是反事实重放。
- 档位信号只看公共信息（对手牌河、副露明细、已胡次数 / 锁手、墙余），本脚本同样不读取任何对手暗手。

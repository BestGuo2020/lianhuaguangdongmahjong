# 血流 LLM 候选与路线修正 — 2026-09-22

## 已完成的确定性修正

问题由 c93b3ed8 完整东风场分析触发。原始文件 SHA-256 为 ec881f341d739a50921f23b26ec6fce1feb38d47f792a42271fe23c8ab3e972b；4局535条命令、121笔结算全部通过真实引擎重放。正式回归只保留四个单座公开视角，位于 src/game/llm/fixtures/bloodFlow-c93b3ed8，不依赖 Downloads 路径，不包含对手暗手或未来牌墙。

1. 从 config.ts 提取既有番值表到 patternDefinitions.ts；计分对象仍引用同一份数值与覆盖关系，所有结算规则不变。五条路线从该表读取番值，修正十三幺和九莲路线仍写16、实际规则已为32的问题。
2. 十三幺/九莲覆盖使用物理牌分配：一张牌最多占一个必需位置；真精可补任意缺位，非精白板只能补白板或精牌面。避免把发财/白板既计自然种类，又当额外精牌增加种类；九莲同时检查基础结构所需张数。
3. LLM 配置开启 routeAdviceOnly：大牌路线作提示，不再额外删除胡、吃碰杠或改张选项。既有规则动作优先级、精牌保护、防守约束仍在前置策略中生效。普通 AI 不启用此候选政策；共享路线番值/覆盖修正同样会作用于普通 AI。
4. 候选增加 income 摘要：立即收入、后续收入、合计、预测模型和展望上限。明确是固定手牌的毛收入估算，未计对手付款与未来再次改张，不冒充净收益或做成十三幺的完整路径价值。
5. 三向听及以上跳过枚举的进张值改为 n/a，并解释未计算，不再把填充0展示成真实无进张。
6. 血流提示词 v3 要求比较即时收入、锁手听口、改张收益和风险；说明路线进度不等于已听/已完成、落后不会增加摸牌机会。移除“覆盖推荐必须在台词说明理由”的冲突要求；不强制模型服从推荐或任意听必改张。实际提示词中的风险与防守信息也使用该请求生效的 aiConfig。

## 回归证据与边界

- routeAccounting 新增4项回归，修复前4项均失败，修复后通过：十三幺/九莲番值、精牌重复占位、打掉幺九后的进度下降、受限白板不能补任意幺九。
- 东一窗口83：胡与任意听改张均保留；当前推荐仍为打一筒。只改善模型拿到的比较信息，未声称真实千问已改变选择。
- 东三窗口173：原先显示“十三幺进度100%”的手牌不再误报近完成路线；没有强制这手追十三幺，模型仍可在合法候选中决定。
- 东四窗口263、墙11且大幅落后：胡/过均保留，路线不再剥夺选择。原有首胡门槛是否改变由独立验证决定。
- 历史 routePrompt 测试显式冻结 legacy 估值与旧硬筛选配置，验证原来的提示词一致性合同；新回放测试另外覆盖生产建议模式。
- 旧 defenseFold 测试把“不弃和”误写成“必须选择高于最低风险的牌”。现在同时断言处于 push、动作等于禁用弃和模式的选择，并打掉十三幺闲张五万，不要求为了证明进攻而打危险幺九。
- terminalSelfDraw 继续覆盖末张自摸强制收益、无合法胡不造胡，以及末张弃牌竞争优先级；正墙余的LLM窗口按新政策保留胡。

## 工程验收

- 全量前端：197个测试文件通过、1个原有跳过；2003项通过、2项原有跳过。
- Vue/TypeScript检查通过，生产构建通过；保留既有大chunk提示。
- 浏览器：三个模拟LLM主题/服务可用性用例、血流大厅可用性、真实worker完整东风场与半庄场均通过。
- 大厅退出重进初跑时，在开发服务热更新期间回到初始大厅，等待手牌超时；源码固定后该用例单独重跑通过。共7个目标E2E均获得通过结果，初次失败如实保留在 work/analysis-c93b3ed8/e2e.log，重跑在 e2e-restart.log。
- 没有真实模型请求、没有线上部署。浏览器使用模拟模型，不能据此宣称实际模型平均牌力提高。
- 独立gate脚本由Vitest执行；额外直接tsc尝试因工作区未配置独立Node类型定义未完成，不计作通过。应用类型检查已通过。

## 策略验证

采用条件和新种子在运行前写入 llm-decision-protocol-2026-09-22.md。真实LLM行为由“跟随实际默认推荐”的离线代理替代，只验证推荐路径；相同牌墙、相同对手、四座轮换、完整东风场携分。两边都包含相同路线正确性修正与建议模式，因此不把差异归因于这些共同变更。

source-v2阶段：计划16种子、64个配对场；10分钟启动预算后完成已在途的配对及计时，实际628.36秒，完成38个配对（9个完整四座种子组，另2个座位）。样本不完整，无置信区间，adopt=false；保留LLM的legacy配置。未追加样本或调整门槛。部分数据不得解读为已证明source-v2更好或更差。

首胡门槛阶段：另一组种子，保留legacy估值，比较40/20/10与0/0/0。10分钟启动预算后完成已在途的配对及计时，实际627.62秒，完成45/64个配对（11个完整四座种子组，另1个座位）。样本不完整，无置信区间，adopt=false；原门槛40/20/10保持不变。没有追加样本或调门槛追求通过。

两阶段均未通过完整样本门槛，部分样本的均值只作诊断，不证明任何方案平均更强或更弱。原协议写最大10分钟，驱动按配对边界检查时限，实际因完成在途配对及后续计时各多约28秒；如实记录，未因此启动额外配对。未来严格墙钟预算应在决策循环内中止并标记未完成配对。

## 本轮结案与未解决部分

采用：番值单一来源、物理牌覆盖修正、LLM路线建议模式、收益口径/未计算特征澄清、提示词v3、四个真实窗口回归。

不采用：LLM切换source-v2；一刀切取消首胡门槛。两项独立探索以证据不足结案。

仍未证明解决：真实模型是否会减少提前锁手；本地首胡门槛导致的反复拒胡。东三窗口130和东四窗口263虽然保留了胡，但当前推荐仍为过。没有实现或验证完整的多步改张净收益搜索，也没有把启发式潜力硬当净收入。后续若研究推荐层，应另立同口径续局估值方案与独立验收，不继续追加本轮样本。

可复查证据：llm-decision-source-evidence-2026-09-22.json、llm-decision-floor-evidence-2026-09-22.json保存两阶段配置与逐场结果；llm-decision-window-evidence-2026-09-22.json保存四个原局窗口的当前/候选比较。

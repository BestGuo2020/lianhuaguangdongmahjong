# Narrow win safeguards: fixed validation protocol (2026-09-22)

Baseline: master 5403ce8 policy, represented by BLOOD_FLOW_LLM_AI with winOpportunityGuards=false. Candidate changes only this flag, using the exact bloodFlowWinSafeguard and bloodFlowDecisionPlan paths. Legacy forecast, ordinary 40/20/10 floors, route advice and all opponents remain identical. No real or paid model requests.

Independent holdout: seeds 6300001–6300008, two each for legacy/attack/defensive/mixed opponents; four focal seats per seed; complete four-round east matches with carry, same deals and fixed opponents in both arms. 32 paired matches. Follow-local-recommendation proxy, with local safeguards applied first in candidate; this checks the late-ron policy and safeguard integration, not how a real LLM would choose among unguarded candidates. The mocked adversarial immediate-win response in replay regression separately checks the any-wait protection.

One fixed run, 15-minute decision-loop budget; no sample additions, retries of adverse samples, parameter tuning or threshold changes after outcomes are inspected. No hidden hands/wall enter policies. Correctness gates: original three failures must pass; adjacent non-applicable boundaries must remain excluded; no illegal commands, stalls or conservation failures. Adoption smoke gates: all 32 pairs complete; paired mean net >=0, first-place proportion not lower, severe-loss (net <= -1000) rate not higher. Report seed-averaged uncertainty descriptively; this small set cannot prove average playing strength. If gates fail, reject this policy version rather than adjust to this sample.

Warm alternating paired timing on up to 128 distinct public views: combined safeguard+recommendation P95 <=2x baseline. Stop after the fixed run; preserve all rows and counts of guard triggers. Application checks: full frontend tests, typecheck, production build, actual single-player worker east/hanchan e2e and mocked LLM pipeline. No P2P changes or deployment.

# source-v2 + 1.2：一次性正式验收

## 冻结对象与结案规则

正式基线锁定在 `728ed400a952802474791ebe8788d4c0e1844917`，独立工作树 `work/source-v2-final-20260920` 用于执行，防止并发代码改动改变比较。唯一候选只启用 source-v2，使用已归档 `opportunity-calibration-2026-09-20.json` 的四个系数和原有 1.2 阈值，不叠加条件模型、公开风险模型、尾部模型或搜索。

| 必须满足的条件 | 门槛 |
|---|---|
| 正确性 | 类型、回归、构建通过；工程游戏无非法动作、卡局或守恒错误 |
| 耗时 | 决策函数 P95 不超过正式版 1.5 倍 |
| 完整对局净分 | 平均增量至少 +50，且按源种子计算的 95% 区间下界 >0 |
| 第一名比例 | 点估计不下降，95% 区间下界 ≥−2 个百分点 |
| 严重亏损率 | 整场净分 ≤−1000 的比例，最多增加 1 个百分点 |

任一条件不满足就拒绝，不调整参数、对手、指标或样本量追求通过。正确性/耗时门槛失败时停止，不继续跑收益场次。只有明确的工具错误才允许留下记录后修复重跑。通过则接入并保留回退，未通过则保留正式默认、归档并结束这条路线。

## 锁定的正式对局清单

四类对手各 64 个新源种子：legacy 4100001–4100064、attack 4101001–4101064、defensive 4102001–4102064、mixed 4103001–4103064。每源四座轮换、每场四局携分，相同牌墙和对手配对，共 1024 场候选和 1024 场控制、8192 局逻辑对局；共同前缀允许复用，但不能省略分歧后的完整后缀。

净分、第一名和严重亏损率都先对同源四座取均值，再进行四个对手分层的源种子 bootstrap（10000 次，随机种子 912701）。并列最高分计第一，与已有工具一致。正式对局清单在测量前生成，工程未通过则保持未运行，不换另一组种子。

## 工程耗时口径（测量前固定）

- 额外使用 4200001–4200016 的 16 个新发牌，四类对手各四个、轮换观察座位；两种策略各自生成完整单局轨迹。
- 对每个不重复的公开座位视角同时测量两种策略，交替调用顺序。选择对象覆盖两种轨迹，不手挑重计算窗口，也不把同一共同前缀重复计时。
- 使用 4300001–4300004 预热运行时，然后清除预测计分/听牌缓存；只计策略函数调用，不把构造视图、哈希或引擎提交耗时混入计时。
- 三个预定的全新 Node 进程串行运行，反转重复间的先后顺序；每次用 nearest-rank 算双臂 P95，取三次比值的中位数判定 ≤1.5。不是挑最低比值，不追加重复直到通过。
- 同时记录最大延迟、机器/Node 信息、全部调用计时和最慢视角。它是同机成对函数基准，不冒充浏览器端到端延迟，也不是冷启动全部缓存的测试。

完整参数、规则、代码指纹、工程和正式种子列表保存在 `source-v2-final-protocol-2026-09-20.json`。正式对局驱动会拒绝工程未通过时运行，也拒绝重复/越界批次或源码变动。

## 当前验收记录

隔离基线全量 161 个文件、1654 项测试通过，原有 1 个文件/2 项跳过；Vue 类型检查和生产构建通过。构建保留既有大 chunk 提示。另用人工可核对的假数据验证结案器的 +50、配对增量、名次与严重亏损边界，2 项测试通过；这些假数据不计入正式样本。

工程测量环境：Node v24.19.0，Intel Core Ultra 5 225H。每遍 508 个不重复公开决策视角，均完成所有工程游戏，无非法动作、卡局、输入修改或分数守恒错误。

| 预定重复 | 正式版 P95 ms | 候选 P95 ms | 比值 |
|---|---:|---:|---:|
| 1 | 105.79 | 107.01 | 1.012 |
| 2 | 103.16 | 120.31 | 1.166 |
| 3 | 105.92 | 104.42 | 0.986 |

中位比值 **1.012 ≤1.5，工程门槛通过**。候选最大单次耗时分别为 341.81、255.59、308.74 ms；最大值保留披露，不事后增加或删除门槛。工程记录为 `source-v2-final-engineering-2026-09-20.json`。

## 完整对抗结论：通过

256 个源种子、1024 个配对东风场全部完成，八个批次均正常退出，无重跑、调参或追加样本。结果如下；比例变化以百分点计。

| 指标 | 候选相对正式版的变化 | 95% 区间 | 判定 |
|---|---:|---:|---|
| 每场净分 | +225.7422 | [157.3730, 293.4131] | 通过 |
| 第一名比例 | +1.2695 个百分点 | [0.0977, 2.4414] | 通过 |
| 严重亏损率 | −1.3672 个百分点 | [−2.5391, −0.2930] | 通过 |

所有预定门槛通过，接入这组精确配置，不叠加其他实验模型。该结果支持在固定四类对手分布上相对冻结正式版有收益提升，不代表所有对手、规则或 LLM 都已验证。

逐场配对数据与判定为 `source-v2-final-payoff-2026-09-20.json`，三遍完整耗时记录为 `source-v2-final-engineering-{0,1,2}-2026-09-20.json`。源代码和运行目录保留在冻结工作树。重现旧验收应使用冻结基线及本次验收脚本；接入后的正式默认不再是本次控制臂。

## 正式接入与回归验收

正式普通 AI 默认启用相同 source-v2 与四个校准系数，改张阈值仍为 1.2。`VITE_BLOOD_FLOW_SOURCE_FORECAST=off` 后重新构建可独立回退到冻结控制配置，保留此前三项已接入修复。LLM 显式固定 legacy、无机会校准系数，不随默认对象展开而改变。历史对抗控制臂与对手也显式固定 legacy，防止后续复现悄悄改变比较对象。

- 接入／回退各在独立进程执行，共 2 项测试通过；正式配置与协议中候选逐项相等，回退配置与控制逐项相等。三遍耗时记录中保存的最慢公开视角全部复现候选决策，回退复现首遍控制决策。
- 接入后全量前端测试 161 个文件、1657 项通过，原有 1 文件／2 项跳过；Vue 类型检查、脚本类型检查和生产构建通过。构建仅保留原有大 chunk 提示。
- 配对回放与判定器 3 个测试文件、11 项通过。
- 实际浏览器 worker 与 Vue adapter 完整东风场、半庄场共 2 项通过（2.6 分钟），每场分数守恒，临时服务已停止。

回归适配记录：历史机会约束用例原先展开生产默认，升级后连历史复现策略也变化，导致其旧动作断言失败。现固定该历史轨迹使用 legacy，并新增新预测下「残局路线不再屏蔽胡牌、交回收益比较」断言，两者均通过；没有通过修改生产策略追求测试通过。Node 归档回放用例放在 `scripts/`，使用脚本类型配置。回退是构建时开关，以两个全新进程验证，不能用同进程切环境变量代表重新构建。

验收原始脚本已单独保存于提交 `a406b20`。严格复现本轮旧对抗应在基线 `728ed40` 使用该提交中的验收脚本；接入后的 `master` 不是旧控制代码，也不应覆盖已冻结的协议重跑。这一轮以通过、正式接入结案，不追加研究实验。

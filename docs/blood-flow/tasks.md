# 血流当前任务

更新：2026-09-06。本轮从干净 master `0a76c91` 开始，前轮实玩冻结版本 `37043a3`，最新收付精简见E13与执行包2；TTS缓存修复及 `95ed115` 节奏/光柱、`bc86580` 听牌网格、`f8352d2` 头像次数、`5078b65` 退场清理均保留。信息减负、分席演出和用户补充的llmAnime位置/字面反馈已实施，证据见验收页；另保留其他任务的 `6eefce3` 听牌提示修复。2026-09-06 又按用户截图反馈：恢复llmAnime gemini角色名「双子星姬」（撤销占位符提交 `ad2d369` 的「美国豆包」暗改）、删除「已听」旁多余的可胡预览卡、恢复思考气泡性格开场台词与TTS并修复进度更新闪烁；随后实施「一炮多响与减负」演出调整（详见下方新节）。vibehub同步、迁移及真实P2P验证继续后置，不开发后端，不调整计分、封顶或锁手规则。

规范：[规则与体验约定](rules.md)。证据：[验收记录](acceptance.md)。本文是唯一持续维护的任务状态表。

## 状态口径

- **已提交 / 自动化通过**：有对应提交和检查记录；不等于视觉、实听和真实 P2P 已全部通过。
- **实施中**：存在未完成工作或未提交改动，不计入完成成果。
- **待修复**：已定位问题，仍需实现和回归。
- **待验收**：实现已有，但该项证据尚未取得。

## 阶段总表

E00～E09 的原清单保存在[首版计划](archive/implementation-v1.md)。E10～E16 的操作要求见[演出与结算细则](design/presentation.md)。不重开已完成的计分开发来修普通交互。

| 阶段 | 范围 | 当前状态 | 后续边界 |
|---|---|---|---|
| E00～E01 | 分支接线、配置、类型和黄金输入 | 首版已提交并记录 | 历史授权仅限已记载具体范围；现行工作流仍读 AGENTS.md |
| E02～E03 | 判型、硬胡、计分、连续胡牌与账本 | 已提交 / 自动化通过 | 响应交互后来由 G01 修正，保留其回归 |
| E04 | 单机、规则 AI、听牌提示 | 已接入；有通用体验缺口 | G03 / G04，AI 策略核心继续复用 |
| E05 | 番型卡、持续盖楼、流水 | 已提交；后续演出已扩展 | 不恢复旧上限；保持 E12 和结算回归 |
| E06 | P2P 权威与恢复 | 本地 mock 通过；真实链路待验收 | 生产开关仍关闭，不能用 mock 代替真实 SDK |
| E07～E08 | 首版演出、动作音、LLM 决策与局末感言 | 首版已接入；后续补齐中 | 普通气泡归 E15；声音实听归 E14-7 / E16 |
| E09 | 模拟、首版集成检查 | 历史批次已执行 | 新代码不自动继承旧次数和整体通过结论 |
| E10 | 本局结算、最终排名、按钮返回 | 已提交 `455c411` / 自动化通过 | 局末感言与过渡由 E15 继续补齐 |
| E11 | 字、牌、光、立绘、收付同一时钟 | `95ed115` 重新安排普通 1900ms、大番 2300ms、顶级 2600ms；单机下一操作与倒计时在演出及摸牌阶段结束后开放 | 已有正常速度实玩与连续帧；不以参数替代实听或用户认可 |
| E12 | 来源飞牌、落楼与常驻牌交接 | `95ed115` 将普通飞行展开为约 450ms、高番约 570ms；实玩触发二响、本家及右家跨层 | 保留持续盖楼、事件去重、积压合并；飞牌回调不提交规则动作 |
| E13 | 各赢家番型、四家胡杠收付 | 数据接入已提交 `15a5b83`；主番分席演出，后续收付按最新反馈仅显示金额 | 每席同笔金额一次反馈；取消赢家摘要卡及重复的番型、倍率、胡牌方式，明细留在流水 |
| E14 | 分档冲击、五主题、非人声效果音 | 字形/位置复修已实施；2026-09-06按用户要求，全部五主题恢复对应非血流原光束/光晕/星芒/粒子，已对照原版画面并通过14项光效/飞牌回归 | 共享时钟、楼位、文字与仅金额收付保留；原声音路由不改；本轮声音样片已获用户实听确认，不扩大到全角色云端TTS |
| E15 | 局末过渡、结算感言、恢复普通 LLM 气泡 | 本地公共路径已回归；局末改用原 winLines 性格台词，不再发 COMMENT 请求；P2P 后置 | 覆盖单机与 P2P；保持局内胡牌无自由感言、动作音不双播 |
| E16 | 对照录像、原版回归、声音、P2P 验收 | 本阶段 master 单机正常大厅实玩一局完成，另补五主题、高番及吃碰杠场景；详见验收页 | 本轮已补公共路径五主题×三尺寸、正常完整一局、两份用户实听及两供应商真模型固定输入；真实 P2P 后置 |

## 通用交互修复

来源：[通用复用检查快照](design/common-behavior-audit.md)。G 编号用于本次检查的跟踪，不替换原 E / R / F 等阶段和场景编号。

| 编号 | 问题与处理 | 当前状态 | 完成条件 |
|---|---|---|---|
| G01 | 吃碰杠胡同窗候选，复用现有操作栏 | 已提交 `7ca6dbb`，7 项浏览器检查通过 | 不先点过；胡优先、多响、抢杠、锁手和末张仍正确 |
| G02 | 普通摸打、吃碰杠及补牌的阶段衔接 | `95ed115` 单机已修：引擎输出阶段边界，复用公共 PACE_MS 及原牌桌动画；正常速度实玩及补杠连续帧已核对 | 单机已完成；P2P 的阶段传递与快照节奏后置。AI 思考仍为独立延迟 |
| G03 | 选牌状态、点击反馈和右端间隙 | `95ed115` 修复同窗刷新清选中和点击音；触屏重复快照用例通过，14 / 11 / 8 / 5 / 2 张回归保留 | 本阶段单机通过；未扩大为真实 P2P 验收 |
| G04 | 接回三类听牌提示 | 单机接线已补齐：后台计算当前听口、逐弃牌听口；按本家手牌缓存，不因别人回合清空。显示已听/可听，恢复弃牌箭头、选牌及悬停预览 | 14 / 11 / 8 / 5 / 2 张计算、碰后无摸牌标记、重复快照与锁手范围已覆盖；保留倍数/余张网格，真实P2P后置 |
| G05 | 倒数提示音与显示 | 已协调操作开放与 deadline；包3补回 didu.ogg，每窗口一次，未开放/默认无倒数静默；用户已确认补充样片可听/静音正常 | 开倒计时的实例在阶段结束后才计时；最后3秒提醒本轮已实听，逐窗口去重/未开放静默回归通过 |
| G06 | P2P 开局收回共享演出 | 待修复 | 两骰、翻精、发牌、开局声等待和收尾与原体验一致；保留权威开局确认 |
| G07 | 普通弃牌报声与后续动作的衔接 | `95ed115` 抽出并复用原报牌完成 Promise，单机落牌阶段等待它；异常等待有上限，离场取消迟到报牌 | 路由/取消回归通过；本轮原报一万样片已获用户实听确认，不扩大到全角色TTS |
| G08 | LLM 公共输入/提示词、配置、人设及条件推理复用 | 本轮完成公共输入/性格基础、候选特征、默认推荐、配置预算与条件深思收拢；DeepSeek/千问真实固定输入验收已做，见验收页 | 共用候选特征与提示词基础、条件深思及预算语义、性格频率/TTS；血流仅覆盖规则/锁手/收益/窗口。见公共能力与 override 清单；普通气泡仍归 E15 |

## 当前推进顺序

### 五包之后的新需求：公共副露与胡牌位布局

**已按最新确认范围实施：四角保持直排并向玩家侧后移，整层避开牌山和机械口；本家操作区、对家头像及局名恢复原样，相机不再变动。高楼出画按用户最新要求接受，不调薄、不截层。** 四家副露带按自身方向远离桌心后移，胡牌位统一到各家右手边两面牌墙交汇处的桌内预留区；普通玩法只放胡的一张，血流在同一首张锚点持续盖楼。具体方向、公共接口、碰杠/手牌让位影响及验收见[演出细则的后续公共布局调整](design/presentation.md#后续公共布局调整副露后移与右手角胡牌位)。先验证静态占地，再接飞牌/光效；前面两个决策话题按用户要求后置。本项为新增需求，不撤销以下五包既有完成记录。

**最新补充：TTS 中点出牌已恢复（G07 / G08）。** 在 `eed865c` 基线上修复 `actBot` 的提交前等待：普通弃牌台词复用公共 `playDecisionSpeech → LocalTtsClient → useAudio`，playing 显示气泡、中点放行；静音/失败放行，提交前重验窗口，离场取消包含中点后的尾音。弃牌台词取用后不再由 observe 重播；吃碰杠仍等裁决确认，胡不新增台词。九类中点边界及模型/气泡回归全部通过（17项批次通过，窗口过期夹具改为明确在合成期间过期后单独复跑通过）；全量1129通过/2跳过，构建通过。此项有独立时序证据，不以此前报牌样片可听代替。见验收页“公共 TTS 中点恢复”。

公共能力后续改造统一遵守[公共能力与 override 边界](design/common-behavior-audit.md#公共能力与-override-边界)。G08已随包5完成公共收拢和真实模型固定输入验收；已恢复的动作/飞牌不重做，选牌/听牌/倒数/结算按实际修改需求渐进收拢，不新增一条大规模重写任务。

2026-09-06 调整：以 `806d1de` 为本次方案核对基线。原版光效已恢复，不等于动作与飞牌公共收拢完成。下面五包替换上一轮“减负后继续精修字效”的推进顺序，仍挂在原 E / G 编号下。详细实施、代码职责和验收见[公共表现收拢方案](design/presentation.md#公共表现收拢2026-09-06-调整方案)。本轮执行证据逐包追加到验收页。

| 执行包 | 对应任务 | 核心交付 | 完成门槛 | 当前状态 |
|---|---|---|---|---|
| 1. 同主题对照与复用盘点 | E11 / E14 / E16 | 核对最新代码；实看优先视频；原版/血流动作与光效对照 | 明确已共用、必要适配、待消除重复；有基线及片段 | 完成：806d1de 基线、参考连续帧、五主题同尺寸样片及 jade/llmAnime 固定对照；14/14 光效/飞牌回归和类型通过，见验收页 |
| 2. 公共动作与飞牌收拢 | E11 / E12 / E14 | 接回原主题动作模板与角色；提取原飞牌公共能力；血流只传来源、楼位、时钟 | 无双播/双牌；普通玩法无回退；血流胡不触发终局 | 完成：双方共用 TableActionCue 与 winningTileFlight；16/16 浏览器、9/9 单测及类型通过，原速/连续帧见验收页 |
| 3. 节奏与信息交接 | E11 / E13 / G02～G05 | 来源→动作→主番/落楼→收付→操作；五状态密度核查 | 原速可辨，按钮完整，下一操作不与旧结果抢焦点 | 完成：普通胡保持1900ms、稳定读番约500ms；五状态原速及47单测/21浏览器用例通过，倒数提示恢复（未实听） |
| 4. 高番、多响与主题复核 | E11～E14 | 单次高番先验，再放枪、多响、连续胡及五主题三尺寸 | 原基础特效保留；逐席身份正确；字可读、无遮挡 | 完成：五主题×三尺寸及高番/多响/跨层，24浏览器/54单测与类型通过；已检查代表帧和小屏原速，用户观感未另确认 |
| 5. 声音、实玩与结算闭环 | E10 / E14～E16 / G07～G08 | 完整人工一局，缺失事件补验，声音/气泡及全部返回路径 | 自动化、视觉、实玩、实听分别有结论 | 完成：正常大厅完整一局及结算续局、两份用户实听、G08两供应商真模型；1127测试/构建和五主题三尺寸结算矩阵通过，证据见验收页 |

顺序执行，每包完成相关回归后再进入下一包；公共组件修改不并行交给多个任务。方案不调整规则、首胡后自动控制或原动作报声。vibehub / P2P / 后端继续后置。

### 上一轮五包记录（历史快照，不再维护状态）

以下保留上一轮交付来源，不能解释为上表的公共收拢已经完成。后续只更新上表，证据追加到验收页。

| 执行包 | 对应任务 / 职责 | 具体交付 | 进入下一包的条件 | 当前状态 |
|---|---|---|---|---|
| 1. 看参考、定展示顺序 | E11 / E14 / E16；本轮执行者必做 | 实际读取优先视频的关键段；在验收页记录来源 → 动作 / 角色 → 主番 → 落楼 / 收付 → 续行，以及本家 / 他家差异 | 有查看区间与连续画面依据，列明采用和不采用的内容；仅阅读旧分析文字不算完成 | 本轮完成：已实际查看全片概览及六段连续帧，补抽核对 1:10 续行；观察与取舍在验收页，声音未实听 |
| 2. 局内信息减负 | E05 / E13 / G04；HUD 与信息组织 | 逐席主番只展示一次，收付仅显示正负金额，合并多笔才标合计；简洁可胡预览、折叠规则说明，保留听牌网格和头像次数 | 等待、选牌、可胡、收付四种状态默认可读；不自动展开明细，不隐藏合法按钮 | 2026-09-06按最新截图反馈继续精简：删除重复番型/倍率/胡牌方式和收付底框；完整事实留在流水，验证见验收页新增记录 |
| 3. 单次胡牌字效与分镜 | E11 / E12 / E14；字效与表现编排 | 先用一个本家高番完成字形、厚边、透视、短重拍、回弹与停留；角色和主字错峰，保留来源与楼位光效，再用同一套结构覆盖其他番型 / 主题 | 正常速度和关键帧均可看清，主字无遮挡，普通胡基线不削弱；无需靠连续出现明细卡解释结果 | 已按两次用户反馈复修：18eb5b2统一赢家席位，0df7e27改为饱满粉紫字面/深紫短厚边。静态前后对照、原速演出和五主题三尺寸已复核；旧位置/软糖材质样本保留为未通过记录 |
| 4. 放枪与多响扩展 | E11～E14；事件编排 | 本家点炮胡 / 本家放枪 / 两响 / 三响分别正确；同一来源一次公共提示，各赢家分席位动作和主番，最后同步收付 | 各家身份、牌型、来源、金额正确；主标题不被立绘盖住，三响不堆三张五行卡，重复胡不积压 | 已实施，五类来源与十连胡固定补验、原速录像/分阶段帧已核对；收付等7/7浏览器检查通过；五主题与小屏进包5 |
| 5. 完整对局验收 | E16；体验检查 | 正常大厅一局；自然未触发的事件用固定输入补验；五主题重点检查角色层，小横屏补查按钮和文字 | 记录真实对局与夹具的区别，旧操作 / 结算 / 重开回归通过；未实听项单列 | 本轮已完成指定前端检查与用户实玩/旁观取证：正常大厅至东2/东3，留有实际胡牌连续帧；吃杠高番多响另作固定补验。五主题三尺寸、原交互及导航回归见验收页；声音实听/整局录像/真实模型感言仍未验证 |

信息展示契约仍在[规则与体验约定](rules.md#局内信息层级)，看片要求仍在[演出细则](design/presentation.md#0-实施前必须核对优先参考视频)。以上历史包号不作为下一轮实施顺序。

### 已完成的可玩性修复作为基线保留

- `95ed115`：普通动作阶段、操作开放与倒计时、飞牌与持续盖楼、普通胡光柱 / 粒子、选牌和报牌衔接已有修复。相关参数仍可根据新演出调整，但不能退回旧 450ms 推进与瞬间飞牌。
- 正常速度实玩东一局，四家胡 10 / 11 / 2 / 0 次，另有高番、三响和五主题补验；证据是[该次提交的记录](acceptance.md#2026-09-05-master-单机可玩性复修)，声音未实听。
- `bc86580`：听牌采用四列牌面网格，每张显示自摸预估倍数与余张，零剩余置灰；逐牌自摸 / 点炮单家金额文字已去除，不再列成待删除项。
- `f8352d2`：胡次数已附着对应头像，移除牌河悬浮入口及“X层”文字，点击仍查流水。保持持续盖楼。
- `5078b65`：退回大厅清理旧玩家与牌桌，重开能够重新接收就绪信号；保留已通过的重开路径。

G04 的完整三类提示、G05 提醒音及 G08 模型外围按影响本轮体验的实际缺口补齐，不扩大为新 AI 策略工程。G06、vibehub 同步及 P2P 测试继续后置，本阶段暂不运行 `pnpm sync:vibehub`。

## 血流后端联机实现（2026-09-08 计划定稿，待开工）

用户三决策：**M1～M4 一起做**、**随机对拍纳入 M1 验收**、**不加观战**（沿用 4 人 + AI 补位与现有重连）。后端权威，前端 TS 引擎为唯一规格源；vibehub / P2P 继续后置。详细文件清单、验收与风险见[后端实现计划](design/backend-plan.md)。

| 里程碑 | 范围 | 验收 | 状态 |
|---|---|---|---|
| M1 规则计分 | `backend/app/rules/blood_flow.py` + `app/core/blood_flow/{decompose,catalog,score,evaluate,win_batch}.py` | 共享 `golden.json`/`scoring.json` 全绿；200 种子对拍（TS vs Python 逐笔 deltas/scoresAfter/nextAction 一致） | 完成：黄金 61/61、对拍 2541 例 0 不一致、边界 4 项 |
| M2 对局流程 | `app/game/blood_flow_engine.py`（锁手/多响/抢杠/杠收付/牌墙耗尽结算/轮庄） | 注入牌墙打完整局；136 张守恒、零和、锁手、多响、墙尽全断言 | 完成：引擎 5 项 + 12 种子整局守恒 |
| M3 协议房间 | registry/rooms Literal 加第三规则集；WS 快照对齐前端 `seatView/protocol.ts`；前端 master 接 `externalAuthority` | 血流双客户端 WS 冒烟；前端协议校验器通过 | 完成：后端房间 3 项 + 前端 `ws/authority.ts` 3 项；真实双端冒烟待 wakudemo 登录环境（生产开关不放行） |
| M4 AI/LLM | 补位 AI 翻译 EV 策略；`candidates.py`/`validation.py` 血流候选 | AI 与前端 EV 同种子一致；LLM 固定输入四条合法 | 完成：EV 策略 7 项 + 房间代打换 EV；LLM 候选层 4 项；房间真实 LLM 请求循环与固定输入验收待联机环境 |
| M5 回归收口 | 后端 pytest 全绿并独立提交；前端 master 提交 + `pnpm test`/build；文档收口 | 两仓库提交与证据记录；不跑 vibehub 同步 | 完成：后端 565 通过（1 例并发 flake 单独复跑通过）、前端 1202/2；两仓库提交与验收页记录完毕 |

## 血流局末台词专属化（2026-09-08 第七轮补充，已完成）

按用户要求：血流局末感言输家台词改为只自我评价、不评价别人；赢家台词不再复用公共库，按最后一次真实胡来源（自摸/点炮/抢杠）用血流专属台词；荒庄沿用共享库；非血流玩法零改动。新增 `bloodFlowRoundLines.ts`（输家 4 性格 × 3 条 + 赢家 3 胡法 × 4 性格 × 3 条），`createBloodFlowReactions` 取用 `bloodFlowRoundReactionLine`。测试 5 项：唯一性/字数上限、不含指代他人字眼、轮换循环、荒庄委托共享库一致、赢家文案与共享库同型不同文。证据见验收页「2026-09-08 血流局末台词专属化」。

## 血流 LLM EV 特征接入（2026-09-08 第七轮，已完成）

在第六轮本地 EV 策略基础上接入 LLM 决策链路（范围与口径见[策略设计的 LLM 接入节](design/ai-strategy.md#llm-决策接入2026-09-08-追加)）：

- 候选注入同源 EV 特征（`features.ev`，由 `bloodFlowEvContext` 单一来源计算）；`engineSuggestion` 改用 `decideBloodFlowActionEv`；模型权限按用户定稿为**可覆盖、要理由**（覆盖时 message 必须简述理由，reasoning 走条件深思气泡）；改张候选只在自摸窗口注入；不新增请求频率与台词通道；`llmEvFeatures` 配置开关（默认开，关闭回退旧提示词）。
- 检查记录：新增 `bloodFlowEvFeatures.test.ts` 5 项；全量单测与构建见验收页；真实 DeepSeek（`deepseek-v4-flash`，activeId 供应商）固定输入四条——摸精改张单吊任意听（选改张）、早局低番拒胡（选过并给理由「这牌还有得打，先看看风头。」）、低番抢杠过（「这牌不急，再看看。」）、抢杠收益可观胡（「这杠抢得值，先落袋为安。」）——全部选择合法、台词带性格。证据见验收页「2026-09-08 血流 LLM EV 特征接入」。

## 一炮多响与减负（2026-09-06 后续演出调整）

按用户截图与说明（参考欢乐麻将红中血流一炮多响的提示方式）实施，覆盖所有主题：

- **出场节奏放慢**：`bloodFlowWinTiming` 普通胡 1900→2300ms、大番 2300→2600ms、顶级 2600→2900ms，impact/readable/score/exit 按比例顺延；本地引擎 `after('win')` 衔接同步使用新时长（含多响 intro），演出期间动作、光效与飞牌仍消费同一时钟。
- **删除来源横幅**：`BloodFlowWinPresentation` 移除「左家点炮 / 左家自摸 / 左家被抢杠 / ·二响」横幅及其样式；点炮、自摸、抢杠的出场动作（立绘、飞牌、光柱、主番、收付）保持原有。
- **一炮多响**：一个弃牌批次被两家及以上胡时，先于点炮者方位显示各主题字效的「一炮多响」特效字 1.5 秒（`BloodFlowCue.introMs`，`cuePhase` 新增 intro 阶段，各阶段标记整体顺延 1500ms，3D 飞牌/光效/主番/收付随之等待）；intro 结束后所有赢家语音同一拍同时播放（`llmAudioBus` 新增组播通道 `playLlmAudioGroup`，独立于串行总线）：llmAnime 用固定胡牌台词、llm 主题按性格用「胡。」「拿下！」等短台词（`decisionSpeech` win 行）、其余主题同时播 hu.mp3；有赢家合成失败时 llmAnime 回落 hu.mp3。
- **思考气泡**：血流条件深思开场恢复性格台词（「等等，让我好好想想。」等）+ 并行 TTS，安全进度到达后原地替换文字；同一 requestId 复用同一气泡 id，修复每个推理块都重建节点导致的闪烁。
- **减负**：删除「已听」旁的可胡预览卡（`blood-flow-preview` 及其数据字段 `BloodFlowTableState.preview`），合法按钮、听牌网格与流水明细保留。
- 恢复 llmAnime gemini 角色名「双子星姬」（撤销 `ad2d369` 的无依据改名），代码、测试、计划文档三处一致。

证据与检查记录见验收页「2026-09-06 一炮多响与减负」。未实听本批新增多响语音；真实 P2P、vibehub 同步继续后置。

## 语音闸门与赢家发声（2026-09-06 第二轮）

用户进一步要求（大前提：接入大模型的 AI 就要说话，与非血流一致）：

- **飞牌与语音并行（最终口径）**：赢家语音与演出完全并行，不做任何挂起/等待（挂起式闸门曾冻结演出与引擎、拖慢整局，已按用户反馈撤销）。台词在模型做出胡牌决定的瞬间定稿并**预合成**，胡牌时刻语音即可开播；一炮多响在 1.5s 特效字后各家同一拍开播；飞牌/主番/收付按原时序进行（多响起飞点按 focus 相对计算，不早于特效字结束）。引擎不等待语音。
- **赢家用自己的台词与 TTS（所有主题）**：胡牌窗口请求模型自己的短台词（`bloodFlowRuntime` 保存 `takeWinLine`，批次提交后取用，回退按性格 win 台词）；llmAnime 用角色固定胡牌台词；其余主题的大模型赢家同样说自己的台词（对齐非血流）；非大模型赢家播 hu.mp3/zimo.mp3。单响立即播、多响 intro 后同一拍播。
- **llmAnime 吃碰杠**：模型气泡与固定台词并存，谁先到谁播 TTS（固定台词随事件先到），不双播；**llm 主题吃、碰、杠播模型台词 TTS**（用户指定，不走本地 mp3 动作音），模型气泡保留；其他主题保持原动作音。
- **杠收付**：杠 cue 由 700ms 加长到 1200ms（score 200/exit 1000），四家杠分可见。
- **收付字号与位置**：赢家金额 clamp(40px,5.5vw,76px)、付款者 clamp(34px,4.6vw,64px)，按参考图（欢乐麻将）居中到各自席位方向（本家/对家居中、左右家靠侧），颜色维持主题。

证据见验收页「2026-09-06 语音闸门与赢家发声」。多响/单响语音与飞牌衔接的实听实看，待用户连同联机对战一起验收。

## 视觉与按钮回退（2026-09-06 第三轮）

按用户要求回退/调整：

- **llmAnime 米色气泡恢复**：删除 2053 行起的「漫画气泡：白纸+墨线」块与 3555 行的 `.llm-bubble` 主题变量覆盖，恢复 1746/1823 行米色气泡（米色底、accent 边框与尾巴）。
- **气泡原始尺寸/位置恢复**：撤销 R2 的 280px 尺寸重定义（删除 `--bubble-*` 变量与两条 `.llm-bubble` 容器重定义），恢复基础 320px/34vw、9px 14px 内边距；**保留** R6.2 移动端避让（气泡移到头像下方、尾巴旋转）与基础移动端缩块。
- **已听按钮改回非血流版本（所有主题）**：血流不再显示「已听/可听」文字标签，llmAnime 显示「听·牌」、其余主题显示 tips 图标；标题/aria 保留状态区分。
- **可胡时无「过」按钮**：血流里本家可胡时不再提供「过」（吃碰杠可胡同窗、抢杠胡、自摸均如此；非血流不受影响）。
- **按钮规则（2026-09-07 定稿）**：锁手前（一次未胡）胡/碰/杠/吃/过五种按钮齐全（该吃就吃、该碰就碰、该杠就杠）；胡牌锁手后**只显示"胡"按钮**（能再胡时），不显示其他按钮；**听牌（已听/可听）按钮始终保留不动**。
- **自动胡已移除、锁手全自动**（2026-09-07 定稿）：`autoHuMs` 1 秒自动胡整段删除——锁手之前任何时候都不自动胡（胡/碰/杠/吃/过五按钮齐全）；本家胡牌锁手后**全自动**：约 `lockedAutoPlayMs`（默认 800ms）后**有胡就胡**（自摸/点炮均可再胡），没胡就打掉摸上来的那张（摸打），锁手的点炮/抢杠窗口没胡就自动过；托管（autoplay）不适用。

证据见验收页「2026-09-06 视觉与按钮回退」。

## 流水组件化与移动端适配（2026-09-07 第五轮）

- **流水改用 `EffectNumber` 组件**（`src/components/commons/EffectNumber.vue`，SVG 金色正数/蓝色负数、斜体 skewX(-8)、按位数自动宽度、超长数字传字符串）：胡牌收付与杠收付四家数字全部走该组件，赢家/付款统一大小（桌面 114、紧凑 60），高低番不再分大小。
- **桌面位置**：本家底部居中、对家顶部居中、下家右对齐锚边（右缘固定向左长）、上家左对齐锚边（左缘固定向右长）；修复了覆盖顺序 bug 导致的动画结束瞬移（`.blood-flow-seat-feedback` 的 transform 覆盖了侧家锚边规则，覆盖规则后移）。
- **移动端位置**：紧凑模式复用 HUD 锚点变量（`--side-seat-anchor`/`--safe-left`/`--safe-right`/`--seat-card-width`/`--hud-gap` 等）——下家/上家贴面板内缘垂直居中、对家 x=50% top=10%、本家手牌区上方；旧固定百分比（侧家 55%、对家 8%）错位已废弃。
- **移动端其他**：无副露手牌居中（原右对齐）；牌与牌间隙 5px→0（牌槽 `width:auto` 跟随牌宽，摸牌位 8px 保留）；对家 profile 横向排布并缩小（107 高→49 高）；对家胡牌特效字移动端水平居中（44%→50%）。
- **杠收付去背景**：杠行同胡行无面板底色/边框。
- **抖动修复**：删除 `EffectNumber` 的 ResizeObserver 重测环（getBBox 测量不含 skewX 斜体外伸，宽度在两值间振荡，上家左锚定行最明显）——只在挂载/值/字体变化/字体加载完成时测量。

证据见验收页「2026-09-07 流水组件化与移动端适配」。

## 血流本地 AI 贪婪 EV 策略改造（2026-09-07 第六轮，已完成）

按用户定稿实施：只改本地 AI（LLM 决策不动）；玩家改张提示只做自摸窗口、抢杠窗口零提示；贪婪最优；抢杠胡同样进 EV 比较（不恒胡）。设计、公式、参数默认值与玩家提示两级设计见[策略设计](design/ai-strategy.md)。计分、封顶、锁手、胡后自动续行规则零改动；P2P / vibehub 继续后置。

| 执行包 | 交付 | 完成门槛 | 当前状态 |
|---|---|---|---|
| 1. 策略层 | `patternPotentials` 番型潜力 + 收益估算；`chainEv` / 改张 / 首胡门槛 / 抢杠 EV 决策；配置参数块；`legacy` 策略键保留 | 新单测 + 关键局面夹具（拒低番胡、摸癞子改张、任意听连锁、残局见胡就胡、首胡=抢杠胡、低番抢杠过）全绿 | 完成：patternPotentials 5 项、EV 决策 9 项（含 8 种子整局守恒）全绿 |
| 2. 接线 | engineWorker / backends 走新策略；lotusAi 只加两个可选回调（patternBonus / safetyExposure），经典玩法零改动 | 全量单测与既有 AI 测试通过 | 完成：lotusAi/ai/engine/seatView 既有 64 项全绿，经典行为不变 |
| 3. 玩家改张提示 | 自摸窗口一行建议 + 点过后手牌徽标（复用 G04 箭头）；抢杠零提示；数据与 AI 同源 | 血流 e2e / 视觉核对，非血流不受影响 | 完成：reformHint 3 项单测 + e2e 3/3（桌面/小屏/抢杠点炮零提示），与 AI 同源公式 |
| 4. 对比报告 | legacy / legacy-min40 / ev 各 100 局自对局 + ev×2 对 legacy×2 混合 100 局 | `records/strategy-ev.md` 产出并写入验收页 | 完成：见 [records/strategy-ev.md](records/strategy-ev.md)。自对局 ev 4037 胡 / 改张 180 次；混合对局 ev 两席 +41180 对 legacy −41180、胡 2402 : 781 |
| 5. 回归收口 | 全量单测、build、血流关键 e2e；文档更新并提交 master | 全部通过；不跑 vibehub 同步 | 完成：全量 1189 通过/2 跳过、build 通过；claim-actions 10 / payments 3 / local 3 / settlement-navigation 7 与新增改张提示 3/3 全过；refinement 组为与本次无关的 3D 冷启动 flake（干净 HEAD 复现，见验收页）；已提交 master，不跑 vibehub 同步 |

## 桌面布局重排（2026-09-06 第四轮，全玩法全主题）

按用户五张参考图（欢乐麻将血流布局）+ 后续逐条代数校对重排桌面几何，解决胡牌位太小。最终口径（用户逐条确认）：

- **牌墙正方形化（四条墙都动，满长升牌槽四角不重叠）**：内侧四条缝隙线的延长线围成正方形（边长 12.96）——侧墙 x=±6.82、近墙 z 5.17、远墙 z -8.47；满长槽（12.5，比牌山多一个牌长）四角互不重叠由墙位保证（槽端距相邻槽 0.026），四角缝隙约半张到一张牌（x 0.53 / z 0.70）。牌山坐标、升牌口、机械口、飞牌来源等全部由牌山权威坐标推导，自动跟随。
- **四角胡牌位（本家右下、下家右上、对家左上、上家左下），与各家升牌槽"左右关系"（同高度带、槽外侧）**：
  - 本家：近槽右端外，(6.7, 5.25)，从左往右，桌面 4 张/层；
  - 下家：右槽外侧竖排，(8.0, -7.5)，从下往上，4 张/层；
  - 对家：远槽左端外，(-8.0, -8.55)，从右往左，4 张/层；
  - 上家（特殊）：近槽左端外，(-6.95, 5.2)，从右往左、3 张/层、列距 0.945（长边沿墙），避开左侧竖排手牌列；
  - 朝向（三条=箭头）各指对面玩家：本家→对家(rot 0)、下家→上家(π/2)、对家→本家(π)、上家→下家(-π/2)；
  - 层距 0.47（=牌高，层间紧贴、像牌山一样码）；普通单张与血流首张共用新锚点。
- **副露/手牌**：本家副露 (9, 7.89) 横排沿近墙右端；下家 (10, -8.45) 竖排右上角；对家 (-9, -10.67) 远墙外侧靠远缘（补杠附牌朝墙侧 +0.685）；上家 (-10.34, 7.3) 竖排左下角。手牌：对家 z -9.53（渲染 -10.53，离远墙 1.21）、上家/下家 x ±9.05（离墙 1.21，三家一致）、本家 2D 手牌；副露让位（concealedMeldClear）按新轨道校验。
- **相机**：默认主题 FOV 39°→45°、视线落点 z +0.4；llmAnime 34°→40°、落点 z -0.2（让本家副露完整入画且画面不过广，机位高度不动）；血流胡牌时相机完全静止（去位移/震动，保留曝光脉冲）；**非血流震屏保留**。

证据见验收页「2026-09-06 桌面布局重排」。

## 截图反馈如何落到 E14 / E16

2026-09-05 用户提供的 jade / llmAnime 两张截图只证明大字、光束、牌和卡片在某一帧出现；没有证明过冲回弹、飞行落点和四家收付的动态过程。不能因此判定动画一定不存在，也不能因此标为已通过。

- 中央主番避开方位与倒数，阅读阶段不混成一组字。
- 本次“绿一色 / 64 倍 / +1920”和下一次“清一色·碰碰胡 / 20 倍 / 单家 200”预览必须标明所属事件并分清主次，不能同时抢焦点。
- 落楼后能看清牌停在哪一层，四家收入和付款分别可见。
- llmAnime 检查实际获胜角色的动作层与同一时钟；桌布上的角色图案不作为立绘演出证据。
- 补完整胡牌前后几秒、带音轨的录像，按“动作确认 → 冲击 → 回弹读字 → 落楼 / 收付 → 交回操作”核对。具体通过项见验收页。
